# Deterministic seed data
The executable seed definition is `src/NeuroPulse.Api/Data/SeedData.cs`. This directory is reserved for student-created dataset variants. Use `POST /api/training/reset` to restore IDs and values.
