# Pact exercise scaffold
`NeuroPulse.Api` consumes the simulator's V1 `GET /api/devices/{serial}/status` representation. Install with `npm install`. Students should add a focused consumer interaction and provider verification; no solved contract is supplied. Switch `/training` to V2 to observe the breaking provider change.
